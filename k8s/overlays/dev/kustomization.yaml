apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: cdms-verify-dev

labels:
  - pairs:
      app: cdms-verify-dev
    includeSelectors: false

resources:
  - ../../base
  - namespace.yaml
  - pvc.yaml

patches:
  - path: cdms-verify-dev.yaml
